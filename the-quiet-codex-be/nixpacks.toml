# Nixpacks configuration for Bun backend
[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build", "bun run db:migrate"]

[start]
cmd = "bun run start"
